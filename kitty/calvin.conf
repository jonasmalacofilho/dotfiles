# Xft.dpi set by GNOME == 96; true monitor DPI == 161.5.

font_family FiraCode Nerd Font SemBd
font_size 10.5

# fn test(x: &mut [i32; 4]) -> impl Iterator<Item=u8> {
#     let x = x.as_ptr();
#     todo!()
# }
