[Unit]
Description=liquidctl automatic configuration

[Service]
Type=oneshot
ExecStart=/usr/local/bin/liquidcfg
ExecStart=/usr/bin/sleep 10
ExecStart=/usr/bin/liquidctl status

[Install]
WantedBy=default.target

